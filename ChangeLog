------ current release ---------------------------

1.1.0 ()
- TD-120  Missing cmakedefine for HAVE_GET_CURRENT_DIR_NAME
          (thanks to Jörg Krause, Ben Boeckel).
- TW-1296 make test/run_all exit with non-zero code if a test fail
          (thanks to Jakub Wilk).
- TW-1845 Cygwin build fails, missing get_current_dir_name
          (thanks to hosaka).
- Implements '--details' to show internal calculations.
- Documentation updates, corrections.
- New URLs.
- Fixed errors in man page
          (thanks to Aleksej).
- Updated source to C++11
- Fixed build warnings.
- Fixed bug with stddev queue removal.
- Fixed bug with exec/signal switch.
- Fixed errors in error messages.
- Now uses common CMake tools.
- Test suite converted to Python.

------ old releases ------------------------------

1.0.0 (2013-02-24)
- Unit test suite.
- Implements '--pid' to signal a program on detection.
- Implements '--execute' to run arbitrary programs on detection.
- Implements '--quiet' to prevent detection messages.
- Implements '--stddev' detection with '--coefficient' (multiplied by sigma)
  and '--sample' (sample size) support.
- Implements '--threshold' detection with '--min' and '--max' bounds support.
- Complete anomaly.1 man page, with examples.
- Uses cmake build system.

Project started February 23, 2013.

------ start -----------------------------------

